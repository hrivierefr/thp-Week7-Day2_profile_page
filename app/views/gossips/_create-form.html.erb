<%= form_for :gossip,  url: { action: 'create', controller: 'gossips'}, :authenticity_token => true do |f| %>
  	Balance ta rumeur<%= f.text_area :content, class:"form-control col-sm-10", :required => 'required'%><br />
  	Souhaitez-vous publier en restant anonyme ?<%= f.check_box :anonymous, class:"form-control" %><br />
  	<%= f.hidden_field :author_id, value: rand(User.first.id..User.last.id) %>
  	<%= f.submit 'Créer cette rumeur' , class: 'form-group text-center btn btn-info'%>
<% end %>